rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Só quem estiver logado com a conta da cantina pode ler ou gravar.
    match /cantina_kv/{docId} {
      allow read, write: if request.auth != null;
    }
  }
}
